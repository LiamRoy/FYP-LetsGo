<?xml version="1.0" encoding="utf-8"?>
    <androidx.appcompat.widget.Toolbar
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/homeToolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#304FFE"
        android:theme="@style/Theme.FYPStayCation"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/profile_image_details"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginEnd="-50dp"
            android:layout_marginRight="-50dp"
            android:layout_toStartOf="@id/homeTitle"
            android:layout_toLeftOf="@id/homeTitle"
            android:src="@drawable/profile_icon_foreground" />

        <TextView
            android:id="@+id/homeTitle"
            android:layout_width="158dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="50dp"
            android:layout_marginLeft="5dp"
            android:layout_marginTop="2dp"
            android:layout_toLeftOf="@id/profile_image_details"
            android:layout_toEndOf="@id/name"
            android:layout_toRightOf="@id/name"
            android:gravity="center"
            android:text="Group Title"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold" />
    </androidx.appcompat.widget.Toolbar>
